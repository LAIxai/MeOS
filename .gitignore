# OS
.DS_Store

# Backups (Git replaces local snapshot files)
*.bak
extension.js.pre*

# Handover snapshots (zip archives of past versions)
引継時のextension/

# Build artifacts
*.vsix
node_modules/
out/

# Editor/workspace-local settings (developer's own work site — never ship to clones)
.vscode/
webview_out*.js
